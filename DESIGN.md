# 未来市公式ウェブサイト UI/UX デザイン規約

## 1. 目的

この文書は、本プロジェクトで UI を新設・変更する際の既定の判断基準である。
機能要件を満たすだけでなく、未来市公式ウェブサイトとして一貫した見た目、操作感、情報の分かりやすさを再現することを目的とする。

対象は次の画面を含む。

- 住民が閲覧する公開画面
- ログイン、パスワード変更などの認証画面
- 職員が利用する管理画面
- メニュー、ダイアログ、フォーム、通知などの共通 UI
- ライトモード、ダークモード、全対応ロケール、デスクトップ、モバイルの各表示

この文書における「必須」は、変更対象の受け入れ条件を表す。既存画面が本規約を満たしていない場合、現在の依頼範囲を無断で広げて全面改修はせず、変更箇所では悪化させない。依頼範囲外の問題は報告し、別対応とする。

## 2. デザイン判断の優先順位

視覚・操作上の判断は、次の順序で行う。

1. 現在の依頼におけるユーザーの明示指示
2. ユーザーが承認したスクリーンショット、プロトタイプ、デザイン案
3. 本 `DESIGN.md`
4. 本規約に適合する既存の類似画面・共通コンポーネント
5. デジタル庁デザインシステム、WCAG などの外部標準

セキュリティ、権限、実装方式、i18n、開発サーバー等については、`AGENTS.md`、`.claude/rules/*.md`、その他のプロジェクト規約も常に満たすこと。

具体的な参照デザインがある場合は、「雰囲気が近い」だけで完了とせず、余白、配置、サイズ、色、境界、状態、レスポンシブ挙動まで比較する。重要な判断が参照資料から確定できない場合は、独自の装飾を追加せず、ユーザーへ確認する。

## 3. プロダクトと利用者

本サイトは、架空の自治体「未来市」の公式ウェブサイトとして、住民向け行政情報と Zoom 製品を利用した相談導線を体験できるデモである。

公開画面では、年齢、障害、IT リテラシー、利用端末、利用言語が異なる住民を想定する。管理画面では、日常業務として反復操作を行う職員を想定する。

UI は次の価値を優先する。

- **信頼できること**: 自治体サービスとして落ち着きがあり、誇張や広告的表現がない
- **迷わないこと**: 現在地、選択肢、次の操作、操作結果が明確である
- **読みやすいこと**: 装飾よりも情報階層、余白、文字、コントラストを優先する
- **取りこぼさないこと**: キーボード、タッチ、拡大表示、ダークモード、多言語でも利用できる
- **一貫していること**: 同じ役割の要素は同じ見た目と振る舞いを持つ
- **失敗から戻れること**: エラー理由、影響、修正方法、キャンセル手段が分かる

Zoom AI、Zoom Virtual Agent、Zoom Contact Center、Zoom Phone への導線は重要だが、SaaS の販売用ランディングページのようには見せない。住民が利用する行政サービスの一つとして、目的と利用結果が先に理解できる表現にする。

## 4. 期待する視覚表現

### 4.1 全体のトーン

目指すのは、端正で現代的な自治体サイトである。平面的な面、明確な罫線、十分な余白、濃紺のアクセント、線画アイコンを基本とし、視覚効果で豪華に見せるより、情報を正しく整理する。

次の表現は、ユーザーから明示された場合を除き採用しない。

- 紫や多色を中心とした「AI らしい」グラデーション
- グラスモーフィズム、半透明パネル、背景ぼかし
- 巨大なヒーローコピー、装飾用の大きなイラスト、意味のない背景図形
- すべてをカード、角丸、ピル形状にするレイアウト
- 多重シャドウ、強い立体感、光彩
- 装飾目的の絵文字や、既存アイコンと作風が異なるアイコン
- 常時動くアニメーション、パララックス、過剰なホバー移動
- 意味のない英語見出し、マーケティング調の抽象表現

公開画面は、罫線と余白を中心にしたフラットな構成とする。管理画面は、操作単位を区切るために `rounded-md` / `rounded-lg` と弱い影を利用できるが、公開画面へ同じダッシュボード調のカード表現を持ち込まない。

### 4.2 ブランド表現

- 基調色は未来市の濃紺 `#00118f` とする。
- ヘッダー上端の濃紺線、市章、サイト名を主要なブランド要素とする。
- 市章はロゴ、主要な区切りなど、意味のある場所だけに使用する。装飾として反復しない。
- Zoom の名称や相談用アイコンはサービス識別に使用するが、未来市のサイト全体を Zoom の製品 UI に寄せない。
- 新しいロゴ、マスコット、ブランドカラーを独自に追加しない。

### 4.3 カラー

色は `app/globals.css` のセマンティックトークンを正本とし、コンポーネント内で同じ意味の色を直接指定しない。

| 意味 | トークン | ライト | ダーク |
| --- | --- | --- | --- |
| ページ背景 | `surface` | `#ffffff` | `#0f1115` |
| ヘッダー・カード等の面 | `surface-raised` | `#ffffff` | `#171b21` |
| ホバー・強調面 | `surface-hover` | `#e8f1fe` | `#1e242c` |
| 標準罫線 | `line` | `#d1d5db` | `#2b313a` |
| 補助罫線 | `line-subtle` | `#e5e7eb` | `#232932` |
| 本文 | `fg` | `#1f2937` | `#e6edf3` |
| 補足文 | `fg-muted` | `#6b7280` | `#9aa4b2` |
| リンク・フォーカス | `accent` | `#00118f` | `#7096f8` |

- 色は役割で選び、見た目だけを理由に任意の Tailwind 色を増やさない。
- 赤はエラー・危険操作、黄またはアンバーは警告、緑は成功・正常状態に限定する。
- 状態を色だけで伝えず、ラベル、アイコン、説明のいずれかを併用する。
- 本文のコントラスト比は原則 `4.5:1` 以上、大きな文字と UI 境界・アイコンは `3:1` 以上を確保する。
- ライトモードで成立していても、ダークモードの通常・ホバー・フォーカス・無効・エラー状態を別に確認する。
- 透明度を下げただけの文字は、読みにくくなる場合に使用しない。

### 4.4 タイポグラフィ

- `app/globals.css` で定義した OS 標準のサンセリフフォントスタックを使用する。外部 Web フォントへの依存を増やさない。
- 基本ウェイトは `400` と `700` とし、微妙なウェイト差だけで階層を表現しない。
- 本文は原則 `16px`、行高 `1.7` 前後とする。補足・操作ラベルは原則 `14px` 以上とする。
- ロゴの欧文表記など装飾的な情報を除き、意味のある文字を `12px` 未満にしない。
- ページタイトルはモバイルで `24px`、デスクトップで最大 `36px` 程度を基準とする。
- セクション見出しは `20px` から `24px`、カードや項目見出しは `16px` から `20px` を基準とする。
- 長文の一行は全角約 40 文字、半角約 80 文字を上限の目安とし、本文幅を画面いっぱいに広げない。
- 日本語の長い斜体、両端揃え、過剰な字間調整を使用しない。
- 見出しレベルは見た目ではなく文書構造に合わせ、`h1` から順序を飛ばさない。
- 多言語で文字数が増えても省略を前提にせず、折り返して読めるようにする。表などで省略が必要な場合は、完全な値へアクセスできる手段を用意する。

### 4.5 余白とレイアウト

- ページの基本コンテナは `max-w-7xl` とし、公開画面と管理画面の左右位置を揃える。
- 本文中心の領域は `max-w-6xl` 以下を目安にし、読みやすい行長を保つ。
- 余白は原則 4px の倍数から選び、`4 / 8 / 12 / 16 / 24 / 32 / 48 / 64px` のリズムを優先する。
- 関係する要素同士の余白は小さく、別セクション間の余白は大きくする。
- モバイルの左右余白は原則 `16px` から `20px`、デスクトップは `24px` から `32px` とする。
- 位置合わせのために空白文字、改行、固定高さを使用しない。
- 画面全体の横スクロールを発生させない。幅の広い表だけは、見出しを保った専用コンテナ内の横スクロールを許容する。
- 主要操作は最初の画面で理解できる位置に置くが、情報を無理に一画面へ詰め込まない。

### 4.6 角、罫線、影

- 公開画面の情報グループ、カテゴリ一覧、記事一覧は、直線的な罫線と余白を基本にする。
- フォーム入力とボタンは原則 `rounded-md`、管理画面のカードとメニューは `rounded-lg` までとする。
- `rounded-xl` 以上はモーダルなど独立した前景面に限定し、理由なく使用しない。
- ピル形状はステータス、トグル、短いタグに限定する。通常のボタンやカードをピル形状にしない。
- 通常コンテンツの区切りは影ではなく罫線を優先する。
- `shadow-sm` は管理画面のカード、`shadow-lg` 以上はドロップダウン、ドロワー、ダイアログなど重なりを示す UI に限定する。
- グラデーションは装飾目的で追加しない。既存のテーマ切替スイッチなど、状態理解に寄与する既存表現は維持できる。

### 4.7 アイコンと画像

- 既存の `app/components/svg/` と `public/` のアセットを優先して再利用する。
- 同じ画面内では線幅、塗り、サイズ、視覚重量を揃える。標準操作アイコンは `20px` から `24px` を基準とする。
- 装飾アイコンには `aria-hidden="true"`、アイコンだけの操作には可視または支援技術向けの名前を付ける。
- 意味のある画像には内容を表す `alt`、隣接テキストと完全に重複する装飾画像には空の `alt` を使用する。
- AI 相談用の多色画像を追加する場合は、ライト・ダーク両方のアセットを用意し、既存の `<name>-dark.png` の命名規則に従う。
- 画像内へ本文や操作ラベルを埋め込まない。

## 5. 画面種別ごとの設計

### 5.1 公開トップ画面

- 共通ヘッダー、主要コンテンツ、ニュース、共通フッター、チャット起動導線の順序を保つ。
- 「情報を探す」をページの主目的とし、Zoom AI 相談と生活情報へ短い導線で到達できるようにする。
- Zoom AI 相談は目立たせるが、派手なキャンペーンバナーにはしない。アイコン、タイトル、短い説明、操作可能な領域で示す。
- 生活情報はアイコンとラベルのグリッドを基本とし、デスクトップでは 6 列、モバイルでは 3 列を既定とする。
- ニュースはデスクトップで 4 列、モバイルで 1 列を基本とし、カテゴリ、タイトル、日付の順序を統一する。
- ヘッダーはデスクトップで主要操作を横並びにし、`lg` 未満ではドロワーメニューへまとめる。

### 5.2 公開情報ページ

原則として、既存の `Breadcrumbs`、`PageFrame`、`PageTitleBand`、`SectionHeading` などを再利用し、次の順序を基準にする。

1. パンくず
2. ページタイトル帯
3. 短い導入文
4. 必要な場合のみページ内目次
5. 内容を表すセクション
6. 問い合わせ先または次の導線

ページタイトル帯は左側の太いアクセント線と淡い背景で現在地を示す。本文中のリンクは、周囲の文章からリンクであることが分かり、ホバーとフォーカスの両方で変化を確認できるようにする。

### 5.3 認証画面

- フォームは原則 `max-w-md` の一列構成とする。
- タイトル、説明、入力、主要操作、補助リンクの順序を保つ。
- 主要ボタンは原則一つとし、幅を揃える。
- 認証エラーで入力済みのメールアドレス等を不要に消さない。
- パスワード表示切替は入力欄と一体に見せ、アイコンの色変化を基本のホバー表現とする。

### 5.4 管理画面

- 公開サイトとは異なり、反復操作の効率と状態把握を優先する。ただし色、文字、フォーカスは共通トークンを使用する。
- ヘッダーはサイト名、管理画面ラベル、機能メニュー、ログアウトを簡潔に並べる。
- ページ先頭はタイトルと説明、主要操作を配置し、その下に検索・絞り込み、一覧またはフォームを置く。
- 一覧は表を基本とし、列見出し、状態ラベル、行単位の操作を明確にする。
- 行操作は三点メニュー等へ集約できるが、メニューを開かなくても対象行が分かるアクセシブルネームを付ける。
- 危険操作は通常操作から視覚的に分離し、確認ダイアログを使用する。
- 作成・更新後の重要情報は、何が完了したか、次に何をすべきかを同じ領域に表示する。

## 6. コンポーネントとインタラクション

### 6.1 共通原則

- 同じ役割の UI を新しく作る前に、既存コンポーネントと既存パターンを検索する。
- ナビゲーションには `Link` または `a`、状態変更には `button`、データ入力にはネイティブフォーム要素を使用する。
- `div` や `span` をクリック可能にする実装は、ネイティブ要素で実現できない場合に限定する。
- クリック領域は見えているラベルやカード全体と一致させ、狭いアイコン部分だけを操作対象にしない。
- 重要なタッチ操作は `44 × 44px` 程度を目標とし、少なくとも [WCAG 2.2 AA の達成基準 2.5.8「Target Size (Minimum)」](https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum)が定める `24 × 24 CSS px` または同等の間隔を満たす。
- 一つのセクションまたはダイアログ内で、塗りのプライマリーボタンを複数競合させない。

### 6.2 クリック可能要素のカーソル

画面に出るインタラクティブ要素の振る舞いについて、以下を確認なしで遵守する。

- `<button>`、`<a href="...">`、`Link`、`href` を持たない `onClick` 付き要素、`role="button"` を持つ要素には必ずポインターカーソルを表示する。
- React のカスタムコンポーネントや SVG 要素でも、`onClick` または `role="button"` を持つ場合は同じ規約を適用する。
- Tailwind では `cursor-pointer` クラスを使用する。`<a href="...">` と `Link` は `app/globals.css` の `a[href]` でも一括して保証し、個別実装の付け忘れを防ぐ。

```tsx
<button type="button" className="... cursor-pointer ...">…</button>
```

- `<a href="...">` と `Link` も、ブラウザ既定値だけに依存せずポインターカーソルを明示する。
- 無効状態ではポインターカーソルを表示しない。`disabled` と `aria-disabled={true}` のどちらでも、最終的な表示は `cursor-not-allowed` とする。
- 無効状態を不透明度だけで表現せず、必要に応じて無効理由を近くへ表示する。

### 6.3 ホバー、フォーカス、押下

- ホバーは要素の種類に応じて最小限にする。
  - 文章中のリンク: 文字色と下線
  - カード・行全体が操作可能: 淡い `surface-hover` 背景
  - 入力欄内のコピー、表示切替などのインラインアイコン: アイコンの色変化
  - 独立したアイコンボタン: 必要な場合のみ淡い背景
- インラインアイコンへ四角い塗り背景を急に出し、入力欄が分割ボタンのように見える表現は、明示された場合を除き使用しない。
- ホバーでサイズ、位置、罫線幅を変えてレイアウトを揺らさない。
- ホバーだけに説明や機能を依存させない。タッチとキーボードでも同じ情報へ到達できるようにする。
- すべてのキーボード操作対象に、通常状態と明確に区別できるフォーカス表示を付ける。
- フォーカス表示は原則 `2px` 以上の `accent` アウトラインまたはリングとし、背景に対して十分なコントラストを持たせる。
- `outline: none` を指定する場合は、同等以上に見える代替フォーカスを必ず設ける。
- 通常の色変化は短い `transition-colors` に限定し、移動アニメーションは必要なコンポーネントだけに使用する。
- `prefers-reduced-motion` では、機能に不要なアニメーションを停止または短縮する。

### 6.4 ボタンとリンク

- プライマリーボタンは `bg-primary` と白文字を基本とする。
- セカンダリーボタンは `border-line` と通常面を基本とする。
- 危険操作は赤を使用し、ラベルだけで操作結果が分かるようにする。
- 「こちら」「詳しく」だけの曖昧なリンク名を避け、文脈がなくても目的が分かるラベルにする。
- `href="#"` の未実装リンクは追加しない。デモ上のプレースホルダーが必要な場合は、現在の依頼で明示する。
- 外部サイトへ遷移するテキストリンクは `ExternalLink` を使用し、原則として新しいタブで開く。リンクには `target="_blank"` と `rel="noopener noreferrer"` を付ける。
- 外部リンクのテキスト末尾には、Material Symbols Outlined の `Open In New` を元にした `OpenInNewIcon` を `currentColor` で表示する。本文中では `16 × 16px` を基本とし、テキストとの間隔とベースラインを揃える。
- 外部リンクアイコンだけを次の行へ孤立させない。アイコンは装飾扱いとし、「新しいタブで開きます」を不可視テキストでリンク名へ補足する。
- アイコンだけのボタンには `aria-label` を必須とし、必要に応じて `title` またはレイアウトを動かさないツールチップを付ける。
- コピーなど即時操作の結果は、成功時に `role="status"` / `aria-live="polite"`、失敗時に `role="alert"` で伝える。

### 6.5 フォーム

- プレースホルダーをラベルの代わりにしない。すべての入力に常時見えるラベルを付ける。
- 補足、入力例、制約、エラーは対象フィールドの近くへ置き、`aria-describedby` で関連付ける。
- 入力エラーには `aria-invalid` を使用し、色だけでなく具体的な修正方法を表示する。
- 必須・任意の表記方針をフォーム内で統一する。
- 入力欄、セレクト、テキストエリアの高さ、角丸、罫線、フォーカスを揃える。
- 送信中は二重送信を防ぎ、ボタンラベルまたは近接表示で処理中と分かるようにする。
- 送信失敗時に修正不要な入力値を消さない。
- 成功メッセージは「成功しました」だけでなく、完了した対象と次の行動を示す。
- 無効ボタンに頼りすぎず、可能な場合は押下後に不足項目を具体的に案内する。

#### 6.5.1 チェックボックス

- 本番コードでは必ず `app/components/Checkbox.tsx` を使用し、それ以外の場所で `<input type="checkbox">` を直接実装しない。
- 可視部分は `16 × 16px`、角丸は `4px`、罫線は `1px` を標準形状とする。形状や寸法を変更する場合は、参照するデザインの実測値またはユーザーの明示的な承認を根拠とする。
- 可視部分の大きさとは別に、ポインター操作のターゲットは最低 `24 × 24 CSS px` を確保する。重要な操作やタッチ操作では、関連付けたラベルまたは操作単位の行・セルを含めて `44 × 44px` 程度まで広げてよい。
- 色は `accent`、`line`、`surface`、`fg` などのセマンティックトークンを使用し、ライト・ダーク両テーマへ適用する。固定色やブラウザ既定の配色だけに依存しない。
- `checked`、`unchecked`、`indeterminate`、`disabled`、`focus-visible` の各状態を視覚的に区別し、`forced-colors` でも選択状態、境界、フォーカスが失われないようにする。
- すべてのチェックボックスに、関連付けた可視ラベルまたは `aria-label` / `aria-labelledby` によるアクセシブルネームを付ける。ラベルや行を操作ターゲットに含める場合も、ネイティブチェックボックスとのプログラム上の関連付けを維持する。

### 6.6 メニュー、ドロワー、ダイアログ

- 開閉ボタンに `aria-expanded` と対象との関連を付ける。
- `Escape`、外側クリック、明示的な閉じる操作を適切に提供する。
- ダイアログとドロワーを開いたら本文スクロールを止め、閉じたら起点へフォーカスを戻す。
- モーダルではフォーカスを内部に保ち、タイトルと説明を `aria-labelledby` / `aria-describedby` で関連付ける。
- 危険操作の確認ダイアログでは、初期フォーカスをキャンセルなど安全な操作へ置く。
- 送信中は意図しない閉鎖を防ぎ、処理結果が確定するまで状態を保持する。
- スティッキーヘッダーや固定チャット起動ボタンが、フォーカス中の要素や主要操作を隠さないようにする。

### 6.7 表、一覧、状態表示

- 表には列見出しを設け、視覚的な配置だけに意味を持たせない。
- 読み込み中、0 件、検索結果 0 件、取得失敗を区別する。
- 0 件表示では、データがない理由と可能な次の操作を示す。
- ステータスバッジは短いラベルと意味色を使い、ピル形状を許容する。
- モバイルでは列を無理に圧縮せず、専用の横スクロール、優先列の表示、カード化のいずれかを選ぶ。
- ページ送り、検索条件、選択状態は、操作後もユーザーが現在地を把握できるようにする。

## 7. レスポンシブデザイン

- モバイルファーストで実装する。
- 最低限、通常のデスクトップ幅と `390 × 844` を実画面で確認する。
- 画面幅だけでなく、文字サイズ 200% 相当でも情報と操作が欠けないリフローを目標とする。
- 固定幅はアイコン、アバターなど必要な要素に限定し、文字を含むコンテナは可変幅と折り返しを基本とする。
- モバイルで単にデスクトップを縮小せず、ナビゲーション、列数、余白、操作順序を組み替える。
- タップ領域同士を密着させず、誤操作を防ぐ。
- デスクトップとモバイルで機能を消さない。配置を変える場合も同じ目的を達成できるようにする。
- 縦向きと横向きのどちらでも利用可能にし、向きを固定しない。

## 8. 多言語対応

`.claude/rules/i18n.md` を必ず満たす。

- 表示文字列はコンポーネントへハードコードせず、`useI18n()` の `t` から取得する。
- 新規文言は `ja` / `en` / `zh-Hans` / `zh-Hant` / `ko` の全辞書へ同時に追加する。
- 日本語だけで成立する固定幅を使用しない。英語や韓国語の長いラベル、中国語の字形でも崩れないことを確認する。
- 日付、時刻、数値はロケールに応じて表示する。
- 言語切替後に文言が混在せず、`html[lang]` と表示言語が一致することを確認する。
- 英語のサブタイトルを装飾的に追加する場合も辞書管理し、主見出しとの重複が強い場合は追加しない。

## 9. ダークモード

- ライトモードだけで色を決めず、セマンティックトークンを通して両テーマへ適用する。
- 面の階層は純黒一色ではなく、`surface` と `surface-raised` の差で示す。
- ダークモードでも本文、補足、罫線、入力境界、アイコン、状態色、フォーカスのコントラストを確認する。
- テーマ切替時にレイアウトを変えない。
- 初期表示でライト画面が一瞬見えるなどのフラッシュを発生させない。既存の `ThemeSync` と初期化処理を維持する。
- ラスター画像はライト用を単純反転せず、必要に応じて専用のダーク用アセットを用意する。

## 10. アクセシビリティ

変更対象は、JIS X 8341-3:2016 の適合レベル AA と WCAG 2.2 のレベル A / AA を目標に設計・実装する。ただし、所定の試験を行っていない状態でサイト全体の「準拠」を表明しない。

最低限、次を満たす。

- セマンティック HTML と論理的な見出し構造
- キーボードだけで到達・操作・離脱できること
- 見えるフォーカス、自然なフォーカス順序、フォーカストラップがないこと
- 文字、UI 境界、状態の十分なコントラスト
- 画像とアイコンの適切な代替テキスト
- ラベル、説明、エラーと入力のプログラム上の関連付け
- スクリーンリーダーへ伝わる成功、エラー、処理中状態
- 200% の文字拡大と狭い幅でのリフロー
- 色、位置、形だけに依存しない情報伝達
- 自動再生、点滅、不要な動きの回避

## 11. UI 変更の進め方

### 11.1 実装前

1. 変更する画面種別と主要利用者を明確にする。
2. 既存アプリを実際に操作し、最も近い画面、コンポーネント、トークンを特定する。
3. 通常、ホバー、フォーカス、押下、無効、処理中、成功、エラー、空状態を洗い出す。
4. デスクトップ、モバイル、多言語、ダークモードで変わる点を整理する。
5. 大きな UI 変更では、`docs/development/codex-development-workflow.md` に従って静的 HTML prototypeを作り、実装前にユーザーの確認を得る。

prototypeは見た目だけでなく、次を確認できる状態にする。

- 実際に表示する文言とデータ量
- desktop、390×844、関連するbreakpoint境界
- light・darkと操作前後、disabled、saving、error等の主要状態
- keyboard、focus、どこがクリック可能か
- 既存画面との接続関係と意図した差分

### 11.2 実装中

- 既存コンポーネントを再利用し、同種のスタイル文字列をコピーして増やし続けない。
- 新しい色、余白、角丸、影を導入する場合は、既存トークンで表現できない理由を説明できるようにする。
- UI ライブラリやアニメーション依存を、単一画面の都合だけで追加しない。
- 機能実装と視覚調整を分離せず、すべての状態を同じ変更内で完成させる。
- 承認済みprototypeまたは実装計画との差異が必要になった場合は、独断で変更せず理由と代案を提示する。

### 11.3 実装後

`.claude/rules/dev-server.md` に従い、Codex アプリ内 Browser で実アプリを確認する。テスト、型検査、`curl` だけで UI 確認済みとはしない。

最低限の確認項目は次のとおり。

- デスクトップと `390 × 844`
- ライトモードとダークモード
- 日本語と、長い文言を含む別ロケール
- マウス、タッチ相当、キーボード操作
- 通常、ホバー、フォーカス、無効、処理中、成功、エラー、空状態
- 主要な遷移、戻る操作、ダイアログやメニューの開閉
- 画面全体の横スクロール、文字切れ、重なり、レイアウトシフトの有無
- Console のエラーと警告、失敗した Network request の有無
- 承認済みプロトタイプまたは参照画面との視覚比較

Browser を利用できなかった場合は、実画面未確認であることを明記する。

## 12. UI 完了条件

UI 変更は、次の条件をすべて満たして完了とする。

- 依頼された機能が期待どおり動作する
- 本規約と、ユーザーが承認した具体的なデザインに一致する
- 既存の公開画面または管理画面の視覚言語から逸脱していない
- すべての主要状態が実装され、操作結果が分かる
- デスクトップとモバイルで情報や操作が欠けない
- ライト・ダーク両方で読みやすい
- 全対応ロケールの辞書とレイアウトが成立する
- キーボード、フォーカス、ラベル、コントラストを確認している
- 既存の共通コンポーネントとセマンティックトークンを適切に再利用している
- 自動検証と実画面確認の結果を別々に報告できる
- 意図していない画面や既存デザインを変更していない

## 13. 参考資料

- [デジタル庁デザインシステム](https://design.digital.go.jp/dads/)
- [デジタル庁デザインシステム: カラー](https://design.digital.go.jp/dads/foundations/color/)
- [デジタル庁デザインシステム: タイポグラフィ](https://design.digital.go.jp/dads/foundations/typography/)
- [デジタル庁: ウェブアクセシビリティ](https://www.digital.go.jp/accessibility-statement)
- [W3C: Web Content Accessibility Guidelines (WCAG) 2.2](https://www.w3.org/TR/WCAG22/)

これらは設計の根拠として参照するが、本サイトへ外部デザインをそのままコピーするための仕様ではない。未来市の既存ブランド、承認済みデザイン、本規約に合わせて適用する。
