/** Settings groups retain their semantics without an ornamental card. */
export const settingsSectionClassName = "min-w-0 space-y-5 border-0 p-0";

/** A single boundary, including the system high-contrast focus indicator. */
export const settingsInputFocusClassName =
  "focus:border-accent focus:ring-0 forced-colors:focus:outline forced-colors:focus:outline-2 forced-colors:focus:-outline-offset-2 forced-colors:focus:outline-[Highlight]";
