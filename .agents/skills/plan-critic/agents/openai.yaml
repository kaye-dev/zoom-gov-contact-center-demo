interface:
  display_name: "Review Plan"
  short_description: "Independently review and make the plan self-contained"
  default_prompt: "Use $plan-critic to review and update plans/<slug>.md."
policy:
  allow_implicit_invocation: false
