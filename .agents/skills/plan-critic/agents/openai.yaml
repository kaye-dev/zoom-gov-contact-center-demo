interface:
  display_name: "Review Plan"
  short_description: "Independently repair a goal and its review-ready prototype"
  default_prompt: "Use $plan-critic to review and repair plans/<slug>/goal.md and its optional prototype."
policy:
  allow_implicit_invocation: false
