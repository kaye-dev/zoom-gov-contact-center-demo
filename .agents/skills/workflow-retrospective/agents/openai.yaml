interface:
  display_name: "Improve Workflow"
  short_description: "Audit one Codex task and prioritize lean workflow fixes"
  default_prompt: "Use $workflow-retrospective to audit codex://threads/<thread-id> and propose prioritized workflow improvements."

policy:
  allow_implicit_invocation: false
