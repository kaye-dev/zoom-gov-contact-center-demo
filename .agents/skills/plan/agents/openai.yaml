interface:
  display_name: "Create Plan"
  short_description: "Create an iterative goal and review-ready UI prototype"
  default_prompt: "Use $plan to create or revise plan/<slug>/goal.md and its review-ready UI prototype."
policy:
  allow_implicit_invocation: false
