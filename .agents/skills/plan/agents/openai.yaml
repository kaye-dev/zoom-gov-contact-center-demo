interface:
  display_name: "Create Plan"
  short_description: "Create an evidence-backed goal and UI prototype"
  default_prompt: "Use $plan to create plans/<slug>/goal.md and any required UI prototype."
policy:
  allow_implicit_invocation: false
