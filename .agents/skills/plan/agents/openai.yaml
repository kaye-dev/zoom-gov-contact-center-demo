interface:
  display_name: "Create Plan"
  short_description: "Create a self-contained, evidence-backed implementation plan"
  default_prompt: "Use $plan to create an implementation plan for this change."
policy:
  allow_implicit_invocation: false
