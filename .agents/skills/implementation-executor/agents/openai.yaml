interface:
  display_name: "実装プラン実行"
  short_description: "承認済み計画をTerra中心で実装・検証"
  default_prompt: "$implementation-executor plans/tmp/<plan-id>/final.md を実行してください。"
policy:
  allow_implicit_invocation: false
