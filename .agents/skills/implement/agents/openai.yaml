interface:
  display_name: "Start Implementation"
  short_description: "Approve, implement, then run one final review"
  default_prompt: "Use $implement to approve plans/<slug>/goal.md, implement it without Browser checks during editing, and run one proportional final review."
policy:
  allow_implicit_invocation: false
