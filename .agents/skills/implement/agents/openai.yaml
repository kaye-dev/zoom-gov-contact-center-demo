interface:
  display_name: "Start Implementation"
  short_description: "Approve by invocation, then implement and verify a goal"
  default_prompt: "Use $implement to approve, implement, and verify plans/<slug>/goal.md."
policy:
  allow_implicit_invocation: false
